---

---

<style is:global>
  :root {
    --h-header: 52px;
    --h-footer: 120px;

    --blockquote-bg: #f9f9f9;
    --pre-bg: #fafafa;
  }

  :root.dark {
    --blockquote-bg: #313131;
    --pre-bg: #1f1f1f;
  }

  svg {
    max-width: unset !important;
  }
</style>
