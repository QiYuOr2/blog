{
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @tabi/site dev",
    "build": "pnpm --filter @tabi/site build",
    "preview": "pnpm --filter @tabi/site preview",
    "build:cli": "pnpm --filter @tabi/cli build",
    "weread:sync": "pnpm --filter @tabi/scripts fetch-weread"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}